
platform :ios, '10.0'
use_frameworks!
inhibit_all_warnings!

target 'renTime' do
  
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'

    target 'renTimeTests' do
        inherit! :search_paths
        pod 'Firebase'
    end
end